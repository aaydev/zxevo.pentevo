#pragma once

// типо emul.h (но не инклудитсо в нем)
//    отдельно пушо emul.h сильно "долго" менять
//    сами переменные нужно объявлять в emul.cpp !!!!




//=============================================================================
// HWND-ы немодальных окон
//=============================================================================

extern HWND hwnd_bp_dialog;		// hwnd немодального окна бряков [NS]
extern HWND hwnd_watchdlg;		// hwnd немодального окна вачесов [NS]

